{
  "name": "application-name",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "postinstall": "blockchain-wallet-service start --port 5000",
    "postinstall": "git clone https://github.com/blockchain/service-my-wallet-v3.git"
    "postinstall": "yarn --ignore-engines"
    "postinstall": "yarn start"
  },
  "dependencies": {
    "express": "3.3.5",
     "blockchain-wallet-service": "0.26.1",
    "jade": "*",
    "paypal-rest-sdk": "~0.6.4"
  },
  "engines": {
    "node": ">=0.10",
    "npm": ">=1.3.x"
  }
}
